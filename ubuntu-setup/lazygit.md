# Lazygit

## Install

```bash
sudo add-apt-repository -y ppa:lazygit-team/release
sudo apt update && sudo apt install -y lazygit
```

## Usage

```bash
lazygit
```

Run inside a git repository to browse commits, stage/unstage files, diff, and push/pull through a TUI.
